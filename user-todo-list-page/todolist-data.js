import { accountsData } from "../home-page/account.js"; 

export let todolistData = [];
// push the todolistData array to the accountsData array where name is equal to name in accountsData
let index = accountsData.findIndex((account) => {account.name === userName});

if (index !== -1) {

} else {
    
}